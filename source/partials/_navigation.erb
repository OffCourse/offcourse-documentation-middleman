<nav class="nav brand-tertiary">
  <%  
    $navigation_items = [
      'Pitch',
      'Possibilities',
      'Explorations',
      'Partners',
      'Writings'
    ]
  %>
  <% for item in 0..4 %><a href="<%= config.http_prefix + '#' + blog('blocks').articles[item].data.anchor %>" class="btn btn--margin btn--nav <% if which_is_link_active == item %>btn--is-active<% end %>"><%= blog('blocks').articles[item].data.title %></a><% end %>
</nav>