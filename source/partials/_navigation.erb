<nav class="nav">
  <%  
    $navigation_items = [
      '<span class="hidden-sm"></span>Pitch',
      'People',
      'Posts'
    ]
    $navigation_links = [
      'anchor-pitch',
      'anchor-people',
      'anchor-posts'
    ]
  %>
  <% for item in 0..2 %><a href="<%= config.http_prefix + '#' + $navigation_links[item] %>" class="btn btn--margin btn--nav <% if which_is_link_active == item+1 %>btn--is-active<% end %>"><%= $navigation_items[item] %></a><% end %>
</nav>